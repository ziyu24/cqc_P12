# P12 项目规则

- 类型：`RESEARCH`。项目范围：研究相邻遥感目标在受控成像退化下由可区分到不可区分的条件，以及检测器出现的 merge、split、miss 等实例分解错误；退化模拟器当前仅是受控研究工具，只有真实观测证据证明普通退化不能忠实预测这些错误时才进入方法开发。
- 修改前先理解现有结构；采用清楚、可维护、可测试的架构，不为假设需求增加框架或抽象层。
- 项目源码只放 `src/`，实验配置只放 `configs/`，其他文档只放 `doc/`。
- 构建工具强制要求的根入口（如 `pyproject.toml`、`requirements.txt`、`Dockerfile`）可以留在根目录；不得借此散放源码、实验配置或说明文档。
- B/C 科学讨论、SERVER科学指令、结果和失败路线只放 `lab/` 的四个固定文件。
- 大型运行产物只放项目运行根的 `runs/`，不得进入普通 Git；数据集、共享依赖和通用权重使用主机固定目录。`pth_data`位于Home还是对应`/dev/shm`由主机配置决定；tmpfs可用于节省Home容量，不得擅自迁移或另造副本。conda 环境优先复用兼容的已有环境，缺包可直接安装；确实无法复用时才用名称创建到 conda 标准 `envs/`，不得在项目、`study/envs`、`runs/` 或 `/dev/shm` 新建环境。
- 项目在 `/dev/shm` 时仍必须是正规 Git 工作区。代码、配置、科学协议和结论必须提交并推送。
- 同名 Home 项目根是持久主项目；`/dev/shm` 只放执行镜像、缓存和大型产物。任何恢复所需的代码、配置、脚本和项目结论都必须由 Home 主项目的 Git 历史及远端 `main` 保存，不能只留在 tmpfs。
- 接手已有工作或判断整个项目时，先读 `README.md` 的范围、`lab/result.md` 的既有结果、`lab/sug.md` 的唯一当前任务，再按当前问题回查 `lab/discussion.md`、`lab/failed_methods.md` 和最近 Git 提交；先用已有摘要定位，不得每轮无差别重扫全部历史。
- 每个任务开始时，尤其是接手聊天记录时，必须先按当前 Git 远端的精确仓库名完整读取公开仓库 `ziyu24/cqc_research_lessons` 当前 `main` 中的 `lesson/<仓库名>.lessons.md`（若存在）；网页或程序可分别使用该仓库的 `blob/main/lesson/...` 与 `raw.githubusercontent.com/.../main/lesson/...` 入口。先理解其中的项目科学问题、实际方法、失败原因和适用边界，再接受历史摘要或旧观点里的科学结论。只读本任务涉及的项目，不扫描整库；没有同名文件只表示尚无已登记教训，不表示没有失败。读取失败时不得假称已经检查。
- 整体判断必须区分累计项目证据与当前任务状态；最近一次失败、停止或新路线暂无指标，不能覆盖已有结果。动态任务号和运行状态只写入 `lab/sug.md`、`lab/result.md`，不得硬编码进 `AGENTS.md` 或 `README.md`。
- B/C 完全同级；`lab/sug.md` 是当前任务单槽，最多只含一个 `rNNN`，不得追加历史任务。Codex按本协议下达任务时必须写出项目名和真实任务号，例如`/goal P12 拉取，完成该项目 lab/sug.md 中的 r004，除关键信息，不要刷屏`，并与`sug.md`唯一小节一致；用户在浏览器ChatGPT中直接交代的自然语言任务不强制改写为`rNNN`。SERVER自行处理路径、资源、环境和重试。
- `gpu_count`必须等于真实参与计算的卡数；用原生分布式或等价入口，不能只暴露两卡。启动前先筛每卡显存需求加余量，再选GPU进程数最少的两张不同卡，0进程优先、同数优先显存余量大者；按每卡不同PID计数，跨卡上下文不算额外训练rank。运行器用`--min-free-mib`传入每卡需求；不足等候，不擅改单卡或迁移在跑训练。
- `COMPLETE`只表示一次命令正常退出，`FAILED`只表示一次命令失败，都不等于科学任务结束，也不得结束当前`/goal`。SERVER把剩余科学步骤组织成已提交的可续接入口，验证已有产物来源与完整性后复用有效步骤，不覆盖证据或默认重训；工程错误自行修复。旧进程退出、项目clean且唯一任务槽仍为同一`rNNN`时，再次调用同一个`cqc-run run start`可续接`FAILED`或`COMPLETE`；旧命令、源提交和运行信息保存在同一`RUN.json`的`attempts`中，日志追加。入口变化用末尾的`--command`提供已提交的剩余步骤命令，不手改历史状态。不为局部成功或工程恢复换号、将耗时预估变成停机门槛或要求用户重发命令。科学结束仍须归纳结果、清空任务槽并提交推送回读；槽已清空或换号时不能续接旧run，`STOPPED`始终不得重开。运行器不自动判科学完成或重跑命令。
- 对以训练或评测为核心的任务，检查强度必须与风险相称；除非发现会使结论无效的科学错误、结构上不可实现或用户 `STOP`，先完成任务定义中的最低成本性能信号再裁决路线。非致命诊断只记录并在同一 `rNNN` 内继续，不得用层层加码的门禁替代核心输出，也不得因此另造任务号或方法名。
- 任务科学结束后，SERVER把充分统计和结论归入 `lab/result.md`，主要失败同时归入 `lab/failed_methods.md`，再清空 `sug.md` 的任务槽；提交并推送全部 tracked 代码、配置和结论，回报远端提交号及该任务明确要求的全部科学输出。缺项或推送失败不算完成。
- 形成可跨项目复用的已验证失败或旧结论修正时，先提交并推送本项目证据，再按 `ziyu24/cqc_research_lessons` 的 `README.md`“项目如何新增或修订总结”把中等粒度科学教训写入 `lesson/<仓库名>.lessons.md`；不得把内部任务号、整份日志、未验证推测或一次工程退出复制过去。
- 项目或新问题的首次验证优先使用 HRSC2016；不适配或数据不可用时可改用其他数据集，并在 `lab/discussion.md` 用一句话说明。
- 主要失败写入 `lab/failed_methods.md`。代码、配置和生成命令留Home Git，大文件可放Home或本项目配置指定的tmpfs；同名项目及相对目录对应，artifacts软链接可双向连接。运行器自动保存`configs/<任务号>.recovery.json`；结果归纳推送、生成材料可取得且零进程引用后，通过`cqc-run run cleanup`删除精确产物；对象、实际位置、删除时间和原因写回同一Home恢复配置并推送，配置及重跑命令一起保留。不另加checkpoint分类或指标比较流程；清理不自动入通用权重库。
- 不在本项目新增项目锁、认领、审批、epoch、角色状态机、多层回执或第二套协调协议。
- 训练默认先用1个预先固定的随机种子；多种子必须先说明必要性、种子数及汇总方式并取得用户明确授权，不能因惯例、顶刊或基准需要自行增加。授权简记现有`lab/discussion.md`，不新增审批文件或状态。单种子不能宣称跨种子稳定性；不得挑最好种子或不断追加到显著。种子数不等于GPU数，单种子仍按2卡规则执行。已有用户明确授权的多种子协议不重复申请，不因本规则停止正在运行的实验。
- 后续每项训练须实际用2卡；其他卡数须说明必要性并获用户授权，省事、缺卡或沿用旧单卡均非授权。B/C交付可执行2卡入口，SERVER核验真实并行，两项各1卡不算每项2卡。保持采样、EMA、全局batch、学习率和优化步语义，无法等价则报告，不擅改协议或中断在跑实验。
- 换执行者或修错不换`rNNN`、不清槽。执行前回复列全训练（含对照、消融、重复及续训），逐项写任务/数据集/基础框架/epoch数/卡数/种子数/预计GPU小时，最后合计；超过12epoch括注实际原因；迭代制列步数及有依据的等效epoch，无法换算明示；预估含全部种子，按卡数×各次运行小时累加，说明依据。预估不写`sug.md`或停机代码，超预估继续，不缩减任务或索要追加授权；旧任务自定限时无效。仅用户明确硬上限生效，此时失败、检查及重跑累计。轮数、步数、数据及科学停止条件不变。
- 每训练臂/种子只留合法验证mAP最佳best及最近完整resume；既定选优指标不改，无合法验证只留latest/既定final，不用test或盲集选优。优先原生hook轮换，不积存每epoch；新写成才删旧，保护best及明确下游依赖，保留优化器/调度器/AMP/EMA等续训状态。正常轮换不另走清理器，积存按恢复/零引用规则清理。改文件不使在跑进程生效，不为此重启。
- 旧产物直接沿用同一恢复配置清理，不补造RUN。输入错记可更正已提交的配置，保留历史；同字节副本的旧映射继续可读。必要恢复依赖保留其实体或生成方式，软链接本身不是备份；不新增目录、登记表、审批或状态机。
