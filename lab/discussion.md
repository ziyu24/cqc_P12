# 科学讨论

## 研究范围与证据锁（2026-09-05）

### 当前主问题

唯一主线是：研究相邻遥感目标在成像退化下何时从可区分变成不可区分，以及冻结检测器随之出现的 merge、split、miss 和重复检测错误。退化生成当前只是制造受控观测条件的工具；不同时开发新检测器、扩散生成器或切片融合方法。

论文级候选假设是：即使一般图像质量和单目标检测表现相近，普通退化模拟仍可能错误预测相邻目标组的实例基数与错误类型；只有该缺口在合格真实观测、未参与校准的场景和检测器上成立，才值得研究受约束的任务忠实退化模型。这是待验证假设，不是项目已有结论。

### 已确认事实

- 当前基线为远端 `ziyu24/cqc_P12` 的 `main` 提交 `012f449b1733773b3039f8abe043ed9087a9a70e`；此前只有初始化内容，没有代码、实验或项目结果。
- `ziyu24/cqc_research_lessons` 当前 `main` 没有 `lesson/cqc_P12.lessons.md`。这只表示尚无已登记教训。
- HRSC2016 提供舰船的水平框和旋转框，规模较小，适合低成本首验；但[已有重标注研究](https://doi.org/10.3390/rs14215460)报告原标注存在小船漏标，因此必须先做相邻组标注资格审计，不能直接把预测数少于真值数解释为不可分辨。
- SODA-A 官方资料报告 2513 幅航空影像、872069 个九类旋转框实例，密度远高于 HRSC2016，可在 HRSC2016 不合格时作为回退；它仍是已经成像和处理后的公开影像，不是真实退化过程的原始场景真值。
- DSR 提供同场景、不同焦距的低/高清对及十个飞行高度，但是否含足量静态相邻目标及可用目标真值尚未确认。RRSISR 提供经配准的真实低/高清遥感图像对，但相邻目标数量、标注与配准精度对本任务是否合格也尚未确认。

### 最近邻限制

- [BSRGAN](https://arxiv.org/abs/2103.14006) 已系统组合模糊、下采样、噪声、JPEG 与相机噪声；仅增加退化种类不构成创新。
- [Meta-Sim](https://openaccess.thecvf.com/content_ICCV_2019/html/Kar_Meta-Sim_Learning_to_Generate_Synthetic_Datasets_ICCV_2019_paper.html) 已用真实数据分布和下游任务目标优化合成数据生成器；仅加入检测反馈不构成创新。
- [SDQM](https://doi.org/10.1117/1.JEI.35.3.033014) 已面向目标检测评价合成数据质量；仅用检测性能评价生成数据不构成创新。
- [DFF](https://arxiv.org/abs/2512.16468) 已比较真实/合成图像下系统决策证据并据此校准模拟器；宽泛的“行为忠实性加模拟器校准”不是空白。
- 2026 年已有工作直接[模拟多级遥感图像退化并测量 YOLO 检测变化](https://doi.org/10.1117/12.3094817)；只画“质量下降—mAP 下降”曲线不够。
- [DSR](https://github.com/IVRL/DSR) 与 [RRSISR](https://doi.org/10.1109/TGRS.2024.3516538) 证明真实配对遥感退化数据并非完全不存在，但它们对相邻实例分解任务的资格仍需样本级审计。

### 首轮方案裁决

考虑过三条路线：直接开发复杂退化生成器、先建设真实配对数据集、先做受控合成 P0。前两条在核心现象和数据资格都未成立时成本过高；采用受控 P0，先判定相邻实例错误是否是独立于普通小目标漏检和 NMS 的稳定现象，同时只做真实数据资格审计。

按照项目首次验证规则，先使用 HRSC2016；若相邻组数量、清晰参考可辨认性或标注完整性门失败，再回退到 SODA-A，并在同一任务中如实记录原因。

### 必须保持的解释边界

- “两个真值目标只得到一个预测”不自动等于 merge；只有单个预测在几何上同时覆盖至少两个真值时才记 merge，其余按匹配结果记 miss、split 或其他错误。
- 可分辨性不是纯图像属性。首轮测量的是给定数据、退化协议和冻结观察者下的条件行为；跨两个检测器一致只能降低模型特异性风险，不能证明物理极限。
- 数字退化结果只能支持 synthetic-controlled 结论。没有同景真实观测和独立真值，不使用 `real-world-faithful`、物理不可分辨边界或传感器性能保证等表述。
- 只有相邻组效应在尺度匹配的孤立目标对照、分数阈值/NMS 扰动和第二检测器下仍存在，且包含稳定的非普通 miss 错误，才继续该科学问题。
- 只有真实配对数据资格门通过，才允许下一阶段比较“图像质量匹配、单目标行为匹配、相邻组行为匹配”或开发受约束校准方法。

## r001 服务器证据复核（2026-09-05）

### 已确认

- 服务器完成了 HRSC2016 的候选组筛选、两种检测器训练、四级受控退化推理、分数/NMS 扫描和 DSR/RRSISR 公开资料审计，并把代码、配置和摘要提交到远端 `main`。
- 服务器报告的方向性现象是：最严重等级主要增加 miss，merge 增量接近 0，duplicate 为 0；这使 H0 仍然是当前更强的科学解释。
- 当前公开 MMRotate 的 Rotated RetinaNet R50-FPN 6x HRSC 参考为 AP50 84.80；服务器选择的 epoch 64 报告 77.75。二者训练集并不相同：公开配置使用 `trainval.txt`，r001 使用 `train.txt`，所以 7.05 AP 差不能单独判为实现复现失败。
- MMRotate 的官方 HRSC 配置明确使用 `trainval.txt` 训练、`test.txt` 同时作为 `val_dataloader` 和 `test_dataloader`。因此 `val_dataloader` 是框架评估角色，不表示底层文件是独立 validation split；r001 实际是 `train.txt -> test.txt`，不是 `train -> val`。
- 主机 `pth_data/readme.md` 的对应索引已有可直接复用的 HRSC2016 Oriented R-CNN R50-FPN 3x 权重：`baseline_oriented_rcnn_r50_fpn_3x_le90/HRSC_trainval_test/best_dota_mAP_epoch_34.pth`，AP50 为 0.9036、状态 valid、可直接 inference。当前 74 项索引没有 HRSC2016 Rotated RetinaNet 条目；第二观察者可使用 MMRotate 官方发布的 6x HRSC 权重，兼容性失败时才按官方 `trainval -> test` 配方重建。

### 改变裁决的协议偏差

1. 汇总器只报告共同清晰队列的数量，没有用该队列筛选主要效应；可复现合成探针显示，共同队列只有 1 组时，加入 1 个不合格组会把应为 100pp 的主要效应错误报告为 50pp。
2. 孤立伪组只按相邻组最小短边匹配，没有执行预注册的逐目标短边、局部对比度和两检测器清晰置信度匹配；bootstrap 又把控制图像的变化挂到相邻图像簇，未按所有原始图像依赖聚类。
3. 推理脚本读取的是框架已经完成 NMS 后的 `pred_instances`；NMS=1.0 只是近似少抑制，不是真正的 NMS 前候选，不能完成原定的网络/后处理归因。
4. 一对一匹配采用按 IoU 排序的贪心算法，却被注释为最大基数匹配。存在两真值、两预测的简单反例，贪心只得到 1 对而最大基数解为 2 对，会直接改变 resolved/miss 事件。

### 对数据划分裁决的修正

- 撤回“使用 best checkpoint 本身足以使 r001 无效”的过重裁决。HRSC 模型库通常按 `trainval -> test` 报告 best；在本受控 P0 中，检测器只是冻结观察者，只要在退化分析前固定、且不按退化效应挑选 checkpoint，就可使用归档或官方 best。
- r001 的 `train.txt -> test.txt` 仍须如实记录，但不再把它作为独立硬阻断。需要修正的是观察者与其参考训练集不一致：优先复用 `pth_data`/官方 `trainval -> test` 权重，而不是用 test 上的 final epoch 替代 best。
- `splits/test.txt` 可继续作为本轮受控退化和统计样本；本轮不把该结果表述为独立外部泛化。若后续开发或选择退化模型，必须另留未参与选择的数据。

### 当前裁决

r001 的服务器执行记录保留，但其科学状态仍为 `inconclusive / protocol_drift`。这项裁决现在只由会改变主要效应或事件归因的四类问题支撑：共同队列未实际筛选、控制匹配与依赖聚类不合规、没有真正 NMS 前候选、贪心匹配不等于最大基数匹配。下一步只做一次低成本纠偏复算：优先复用 `pth_data` 或官方权重，不改变数据、退化和原始门槛；若修正后仍失败，才确认 stop。

## B：服务器实查、证据修正与顶刊路线（2026-09-06）

### 结论及证据边界

当前值得投入的是一次不重训检测器的有效首验，尚不足以投入顶刊方法开发。累计证据是已有样本、两个冻结观察者的资产和一轮有偏的方向性数字；当前任务状态则是任务槽已空、纠偏实验没有产生效应统计。不能把工程完成、历史 stop 或当前无指标分别解释为科学成立、假设被否定或项目没有积累。

本次拉取并实查的远端与 46 服务器 Home 主项目均为 `5208210`，工作区干净。按精确仓库名请求 `cqc_research_lessons/main/lesson/cqc_P12.lessons.md` 返回 HTTP 404，没有读到同名教训。只读访问了 P12 产物、当前推理依赖和配置指定的共享权重，没有进入索引中记载的旧来源项目。服务器有 4 张 A30 24GB，检查时均有计算负载；资源瞬时状态不作为科学方案约束。

### 本次实际复现的问题与恢复证据

可执行审计为 `src/audit_controlled_evidence.py`。在 46 原有 d3 环境，以 `--config configs/r002_controlled.json --source src/run_r002_controlled.py --load-observer` 执行；仅 CPU 读取，不改变环境或 checkpoint。

- **权重兼容可恢复，已经实测。** 对 checkpoint 的 NumPy metadata 使用局部 Unpickler，只把 `numpy._core.multiarray` 的 `_reconstruct`/`scalar` 映射到当前 `numpy.core.multiarray` 对象；没有全局 `sys.modules` 别名。348 个 state-dict 值均为有限张量，向归档配置创建的 Oriented R-CNN 执行 `strict=True` 加载，全部键匹配。权重 SHA-256 为 `9a22a0d164b20c054d0ab91f06c12ce88eb3f3611430ffb2653dc05b7fc1412c`。这只证明反序列化与参数结构兼容，**没有执行前向或测量 clear AP50**，不能直接认定观察者已完整复现。适配器是兼容代码，不是安全受限的反序列化器，仅用于可信归档权重。
- **生产汇总器有确定性错误，已经实测。** 构造一组 clear 正确、退化后只在相邻侧漏检的完整四级记录，真实应得效应 100pp；实际 `summary()` 得到 NaN，依赖图明明有 1 个分量，汇总却报告 0 个。原因是 `dependency_components()` 返回整数行索引，而汇总字典以字符串 `record_key` 索引。修正后还要保持“按组平均”的估计量：抽到一个依赖分量时携带其全部组，按组数计算分子/分母，不能把大小不同分量的均值等权平均。
- **NMS 前接口不兼容，已经核实安装代码。** 归档 ORCNN 使用的 `mmdet.Shared2FCBBoxHead` 没有 `_bbox_post_process`，现有统一 hook 会出错；RetinaNet 的该函数同时负责把框缩放回原图，直接替换为 identity 会跳过坐标恢复。两类观察者必须分别提取候选，并验证离线 NMS 能复现标准推理。ORCNN 最终 RoI NMS 前候选仍在 RPN 筛选之后，不能据此声称检查了全部网络前端信息。
- **控制样本总容量足够，共同支持仍未知。** 100 组/86 幅相邻图包含 240 个目标；排除相邻源图后有 356 个孤立目标，来自 271 幅源图。271≥240 说明“一幅控制图最多一个控制目标”的匹配在总容量上可行，不保证四协变量平衡或最终共同队列合格。
- **静态代码复核还发现两处协变量偏差。** 实现使用 `log(sqrt(area))`，协议要求 `log(short_side)`；局部背景环只填外边界，未扣除环孔，实际混入目标内部。恢复既定短边和背景环语义，并报告在最终主队列内的平衡情况。原自测的共同队列与同一 NMS 候选两项分别只是均值恒等式、集合恒等式，没有调用实际队列筛选或候选流水线；五个 True 不能作为整条证据链已通过的证据。

被审计源码 SHA-256 为 `86692c186b895fde714826aeee4d1e4c9303beed2efe021e23ae9ae7fcb8283a`，样本 population SHA-256 为 `5183221e235c505d87d8f5e6947175140ba57c37641eb92ec27c84e313d86137`。这些是实现诊断，不是新的退化效应实验。

### 顶刊可能性判断：有条件的研究方向，尚无论文级证据

以遥感方法顶刊为目标时，TGRS 比立即承诺 TPAMI 更合理；若真实跨成像条件证据和应用意义充分，再评估 ISPRS Journal of Photogrammetry and Remote Sensing。期刊定位是当前研究判断，不是录用概率。TGRS 官方明确要求方法进展、重要研究及完整实验条件，现有工程诊断尚不满足这一要求：[TGRS 范围](https://www.grss-ieee.org/publications/transactions-on-geoscience-remote-sensing/)。

本次一手资料检索补充了两个必须面对的近邻：

| 已有工作 | 已覆盖内容 | P12 必须超出的部分 |
| --- | --- | --- |
| [Kitware 2023 传感器性能研究](https://www.kitware.com/geoint-2023/)、[pyBSM 文档](https://pybsm.readthedocs.io/en/stable/index.html)、[NRTK 2026](https://www.kitware.com/how-to-stress-test-your-ai-models-without-collecting-new-data/) | 按传感器/环境参数扰动影像，并测量检测性能与失效范围 | 不把物理退化、参数扫描或失效曲线本身当创新；必须揭示普通质量/单目标指标无法解释的邻接额外风险 |
| [Meta-Sim，ICCV 2019](https://arxiv.org/abs/1904.11621) | 以分布差异和下游任务表现优化数据生成 | 不能仅把检测损失接到模拟器上 |
| [DFF，2026-01 修订预印本](https://arxiv.org/abs/2512.16468) | 在真实/合成配对中比较决策证据，并用其校准模拟器 | 若提出实例行为校准，必须证明它超出普通输出/决策忠实度，在未用于校准的场景与观察者上仍有效 |
| [遥感质量退化与 YOLO 检测，SPIE 2026](https://doi.org/10.1117/12.3094817) | 模拟不同遥感质量并测量检测变化 | 四级合成退化加两个检测器、再画 mAP 曲线不足以构成顶刊主贡献 |

未检索到完全等同题目不能证明新颖性。当前最值得保留的论文问题是：**在单目标表现相近时，目标间隙相对于有效模糊宽度的变化，是否产生可预测、跨观察者的实例分解额外风险；普通模拟能否把这种风险从受控数据迁移到真实观测？** 间隙/模糊宽度、像素短边、对比度和方向只是假设中的解释变量；数字退化的高斯 sigma 不等于真实成像系统的总 PSF，也不能把条件检测边界称为物理分辨极限。

现有四级退化同时改变模糊和采样，不能分离两者作用；自然相邻与孤立目标之间也可能保留未测量混杂。因此，首验只能支持指定协议下的条件额外风险。若首验通过，后续机制实验应分别改变模糊与采样，使用小规模正交组合和预先定义的自然间隙分层，先检验短边、对比度之外的增量预测价值；不能从四个共变等级拟合出所谓普适分辨定律。

### 从有效首验到论文的条件路线

1. **先得到可信性能信号。** 继续使用 HRSC2016、既有 100 组、同一四级退化和两类冻结检测器；先完成 clear 与最严重等级的匹配对照结果，随后补齐中间等级、原定分数/NMS 敏感性和真正 NMS 前证据。不新增检测器训练或复杂生成器。首验成功只获得继续研究资格，不等于顶刊证据。
2. **核心现象成立后，先验证迁移与真实桥接。** 使用一个更密集且有可复用基线的独立遥感集（SODA-A 或 DOTA，按数据与权重条件择一），按原图/场景划分，先冻结分析再计算；最终应加入一个未参与拟合、不同检测范式的观察者以检验模型特异性。这里是后续路线，不是当前任务扩展。真实配对优先对 [DSR](https://github.com/IVRL/DSR) 取得样本后核查静态实例、独立 HR 数量标注、配准残差和重复场景，不能只复读网页。公开数据没有检测标签不等于永远不合格，可以独立标注补足；仍未核验的项目必须保持未知。RRSISR 的 DOI 元数据已复核为《Toward Real-World Remote Sensing Image Super-Resolution: A New Benchmark and an Efficient Model》，本次未取得新样本级证据，不提升其资格。
3. **真实桥接合格后再决定是否需要方法。** 固定“普通模糊/采样”“传统复合退化”“有足够元数据时的 pyBSM”等对照；在同一校准场景和参数预算下分别按图像质量、单目标行为、相邻组事件分布校准。只允许成像参数改变，不允许生成/移除实例。首先检验前两种匹配能否预测真实邻接风险；若已经足够，则不开发额外模型。只有存在稳定缺口，才研究低维、受成像约束的实例行为校准。
4. **论文级证据必须落在未见数据。** 校准、验证、测试按独立场景分开，同景裁块/不同高度/低高清配对必须在同一分区。主要比较是真实 held-out 场景上的完整分解率预测误差、基数 MAE、merge/duplicate/miss 概率误差及校准误差，而非只比较 mAP。报告相对于最佳简单对照的效应与场景簇置信区间；用独立场景数量做样本量设计，不能把大量邻近裁块当独立样本。若只在拟合用检测器或模拟条件有效，不作跨观察者或真实世界忠实性主张。

若有效首验仍主要是普通 miss、原继续条件不成立，停止当前“独特实例分解错误→任务忠实退化模型”路线；这是研究投入决策，不是统计上接受零假设。若效应方向正但区间很宽，报告精度不足，不能写成不存在效应；是否另做精度研究需由这些结果决定，不能靠换数据、调门槛或换方法名称延续原主张。

## B：完成报告复核与正确观察者恢复（2026-09-06）

### 实际结论

服务器新增了四级条件统计，是真实进展；但不能验收“核心科学输出完整”。在收到完成报告后，本地与 46 均拉取至来源提交 `8a14272`，按精确仓库名读取的教训入口仍返回 404。本次没有把小队列和异常 AP 直接当作科学失败，而是核查了实际权重 metadata、安装代码及完整清晰集。

关键根因是冻结观察者身份未真正保持：MMRotate 0.x 的 RetinaNet 锚框角度恒为零，而本项目使用的 1.x 配置将长宽/角度规范化为 `le90`。虽然参数张量可以加载，网络回归量依赖的锚框语义已经改变。[MMRotate 官方锚框实现](https://github.com/open-mmlab/mmrotate/blob/1.x/mmrotate/models/task_modules/prior_generators/anchor_generator.py) 明确要求，直接加载 0.x 权重时锚框 `angle_version=None`。只改这个参数，框解码器的 `angle_version='le90'` 保持不变。

在固定 test 前 20 图中，只改变锚框配置，score=.25/IoU≥.5 的匹配数从 10/25 恢复到 23/25；该检查只用于验证已知兼容修正，没有扫描或择优选择参数。继而完整 453 图复测如下：

| 观察者 | 官方 VOC07 AP50 实测 | 对应参考 AP50 | 100 组中的 clear 完整分解数 |
| --- | ---: | ---: | ---: |
| 归档 Oriented R-CNN | 90.3558% | 90.36% | 96 |
| 官方 RetinaNet，正确旧版锚框语义 | 84.7564% | 84.80% | 75 |

两观察者共同 clear 队列为 **74 组/63 幅原图**，不是原报告中的 27 组/23 图；这只是按继承接纳名单计算、尚未进行匹配平衡/共同支持筛选的资格数。两份权重都严格加载，使用原 test 453 图和 1228 个 GT，评测直接调用 `DOTAMetric` 所用的 `eval_rbbox_map`，IoU=.5、VOC07 11 点 AP。原自定义梯形积分得出的 ORCNN 95.85% 与官方指标不可直接比较。完整证据、校验值、重建命令见结果文件的 B 复核补充。

单图的“自定义候选回放=标准推理”不能单独证明观察者兼容：两条分支可能共用同一错误锚框配置，因此一致地产生错误预测。本次完整 AP 与归档参考对齐，才排除了这个具体的观察者失配。该教训限制的是观察者资格判据，不是否定相邻实例假设。

### 已完成与仍未知

- 已完成：旧权重锚框兼容定位；正确配置的完整清晰集官方 AP；原 100 组上的共同 clear 数量；修正配置与可重建审计脚本已提交。
- 仍未知：正确 RetinaNet 退化后的相邻额外风险、重新匹配后的最终队列及其 SMD、主效应和非普通 miss 事件是否满足原继续条件。原四级 RetinaNet 输出是在错误锚框下生成，不能只换分母或重算区间来修复。
- 原 `acceptance.json` 由程序为全部组写入统一的 accepted 和盲审说明；本次只继承名单计算队列，没有重新独立核实人工审阅过程，不能把程序产生的说明当作新增人工审计证据。
- 原完整统计只对主设定计算 bootstrap，NMS 前匹配覆盖和覆盖损失未输出，最终主队列 SMD 也未独立报告。这些是原任务要求的缺项，不是新增的科研门槛。

### 对现有实验的最小补齐要求

继续相同科学问题、100 组名单、两份冻结权重、四级退化与原阈值，不因本次兼容修复另造科学路线或方法名称。旧产物和完成记录保留，新计算以补充产物保存，不覆盖旧证据。执行编号的具体交接以结果文件和唯一任务槽为准。

1. 将明确的旧权重兼容配置用于 RetinaNet，重新生成 clear 与所有退化等级的候选；ORCNN 配置和预处理未变，既有候选经来源核对可复用，避免重复推理。官方 clear AP 已得到有效结果，无需继续重复做环境或观察者资格检查。
2. 用正确 RetinaNet 的 clear 置信度重新做全局控制匹配；不得继承基于错误观察者产生的旧控制选择。报告完整样本流、最终共同支持/主队列的四协变量 SMD 和依赖簇规模，不能直接把 74 组计数当作最后分母。
3. 优先产出主分数/NMS 的四级效应和事件置信区间，然后补齐既定分数/NMS 敏感性、真正 NMS 前最大匹配覆盖与最终 NMS 覆盖损失、实际生产反例及完整合格集补充。原始重复候选不作为 NMS 前资格失败依据。
4. 原有逐组审阅依据存在时引用并复用；没有可核对依据的候选才补核。最终输出明确区分数据审核、观察者恢复和退化科学结果，不用其中一类替代另一类。

顶刊判断不升级：目前恢复了可用的实验基础，尚没有正确观察者条件下的正向机制证据。先把已有实验补齐，才能判断是否值得进入独立数据或真实配对阶段；现在没有依据扩数据、重训检测器或开发生成器。

## B：下一步科学投入与顶刊证据结构（2026-09-06）

本轮已完整读取公开教训库当前 main 的 `lesson/cqc_P12.lessons.md`；它确认的是“加载与回放一致仍不能保证观察者身份”，并明确正确观察者下的科学效应尚未知。本次只读核对 46 主项目及既有产物，没有获得新的退化指标。当前任务应直接补齐该指标，不能把已恢复的 clear AP 当作论文进展重复检查。

### 当前实验的投入边界

既有 ORCNN 候选可以省去重复推理，但新 RetinaNet clear 置信度会改变控制匹配。新控制源图可能没有旧退化缓存，因此必须按观察者、原图和等级核对覆盖，仅补缺失输入；不允许通过丢弃这些控制改变样本。全体 100 组匹配后的 SMD 不能替代最终共同 clear 队列的平衡。主结果采用按组平均的双重差，目标级 recall 另用目标分母；依赖分量大小按组计，不能计入重复条件条数。

本次保留原继续判据，完整呈现全部条件的点态区间。这个判据用于选择是否继续投入，不能把在多个等级、事件中找到一个满足条件的结果称为经过多重比较控制的确认性发现。核心信号成立后，下一份独立数据协议必须在看结果前冻结主要等级或整体检验、主要非 miss 事件和效应方向；不能将本次选择过的条件再次当作未选择的验证。

### 结果成立后的三项研究工作

以下是未来投入顺序，尚不是已下达的服务器实验，也没有预先宣称成立。

1. **机制分离与独立复现。** 使用一个具有可复用冻结基线的独立密集遥感数据集，按原图/场景留出。先做小型正交实验，例如 sigma∈{0,0.8,1.6} 与面积下采样倍数∈{1,2,4} 的九种组合，包含纯模糊、纯采样与交互项；实际跨数据尺度在见到该数据的退化结果前固定。比较仅含短边/对比度/clear 置信度的简单模型，与加入自然目标边缘间隙、方向及间隙和退化交互项的模型，在留出场景预测相邻额外风险是否有增益。保持简单低维模型和同一拟合预算，不把共变四级曲线拟合成普适定律。自然间隙仍可能与场景混杂，结论限于预测解释；若要主张因果机制，还需要更严格的成像/布局干预。
2. **真实观测桥接。** 对 [DSR 官方数据](https://github.com/IVRL/DSR) 先取得少量实际配对样本，核对目标类型、静态实例身份、独立 HR 数量标注、配准误差相对目标短边的量级及可独立划分的场景数。官方资料确认的是不同焦距、不同高度的同景配对，不保证适合当前舰船观察者或相邻实例统计；对象域不匹配时不能强行复用舰船模型。没有现成检测标签时可独立标注，但应报告真实标注工作，不能由模型预测生成自己的真值。同景不同高度、低高清版本及裁块必须归入同一分区。以当前效应区间和场景内相关性规划场景数量，不能只凑裁块数。数据资格不足时保留 synthetic-controlled 边界，不宣称真实忠实性。
3. **仅在简单基线解释不了真实风险时做方法。** 同一真实校准集、相同参数维度与评估次数下，比较普通模糊/采样、传统复合退化及在元数据足够时的成像模型；分别按图像质量、孤立目标行为、相邻组事件校准。最小方法候选是低维、受成像约束的参数校准，不允许增删实例。开发前先检验图像质量或孤立行为匹配是否已经能预测真实相邻风险，若能则无需额外方法。最终在未用于调参的真实场景和至少一个不同范式、未参与校准的观察者上，比较完整分解概率误差、基数 MAE、merge/duplicate/miss 概率误差；采用场景簇区间，mAP 仅作辅助。迁移必须超过最佳简单对照，不能只证明在拟合用模型上更接近。

### 投稿定位与可证伪主张

工作判断：优先把 TGRS 作为有条件目标。其[官方范围](https://www.grss-ieee.org/publications/transactions-on-geoscience-remote-sensing/)要求方法进展、重要研究及完整实验条件；当前清晰复现加四级受控检测曲线还不够。可争取的贡献是“单目标或常规图像质量匹配会遗漏的相邻实例风险，以及能够迁移到真实未见场景的解释或校准方法”，需要上述证据逐项支持。若真实跨成像条件证据和应用意义充分，可再评估 ISPRS Journal；这不是录用概率判断。

若有效补算仍主要表现为普通 miss、只依赖某个 NMS 阈值，或原继续条件不成立，当前特异性路线应按原约定停止投入并保存负证据。若方向正而区间宽，说明精度不足；不会把它写成假设已被证伪，也不靠改变样本、阈值或标题自动续做。独立复现、真实配对资格和方法优于简单基线均是未知，不在当前任务里预支为成功。

## B：正确组级估计量复核与当前投入决定（2026-09-06）

最新完成报告确实带回了正确观察者的退化预测和最终平衡队列，这是已有证据的实质增加；但源码把“整个孤立伪组是否检出失败”换成“组内平均目标失败率”，两者不是同一个 estimand。相邻端是二值组失败，控制端也必须是同大小伪组的二值失败。最简单反例是一个两成员相邻组和两成员控制伪组都各漏一个：两端组失败相同，额外风险应为 0；减去控制平均漏检率却会得到 50pp。这不是多做一个检查的问题，而是问题本身被改变了。

本次直接使用保存的逐组事件，在 CPU 上补齐正确统计，无新模型推理、无阈值修改。74 组、63 个源图依赖分量、172 个唯一控制目标的同一队列下，最严重退化的 ORCNN 效应由原报 3.266pp 变为 -8.108pp [−19.718,2.703]，RetinaNet 由 14.302pp 变为 1.351pp [−14.103,16.438]。九种后处理中该等级 ORCNN 效应范围全为负，RetinaNet 上限仅 6.757pp；其他主等级也没有两观察者共同达到 10pp。相邻目标确实发生更多 miss，但匹配孤立伪组也失败，不能把普通退化损伤直接归因于邻接特异机制。分开的事件统计、覆盖损失和所有条件区间见结果文件。

投入判断因此仍是不继续当前“相邻实例特异风险→任务忠实退化模型”路线。不能用原报告里显著的 RetinaNet 14.302pp 支撑顶刊主张，也不把纠正后的不显著或负值包装成普适物理结论。尤其 RetinaNet 的区间仍包含有意义的正风险，ORCNN 在中等级也未精确排除正效应；本次是未达到既定投入门槛，而非证明所有相邻风险不存在。前文提出的独立数据、真实配对与方法阶段仍是条件路线，本次结果没有触发它们，不生成下一轮训练任务。

协议验收与这个投入决定分开：服务器报告中三个所谓验证项是常量 True，本次已用实际生产统计反例替换；100 个审阅图路径存在，但不独立证明人工盲审过程；记录只覆盖 74 个共同 clear 组，不能冒充全体接纳组的无条件补充。本次可以据继承队列上的核心结果停止投入，却不能声称全套服务器科学输出无缺项。上述边界应随负结果一起保存，未来只有研究问题、数据资格或独立证据实质变化时才重新评估，不靠再换一个任务名续做同一路线。
